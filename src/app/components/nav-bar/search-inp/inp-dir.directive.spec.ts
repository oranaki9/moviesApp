import { InpDirDirective } from './inp-dir.directive';

xdescribe('InpDirDirective', () => {
  it('should create an instance', () => {
    const directive = new InpDirDirective();
    expect(directive).toBeTruthy();
  });
});
